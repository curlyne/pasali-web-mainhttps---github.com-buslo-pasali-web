"use client"

import GenericRename from "@/components/form/generic-rename"

export default function ToolbarRename() {
  return <GenericRename noun="Announcement" defaultValue="" onAction={async () => {}} />
}
